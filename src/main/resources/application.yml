server:
  port: 8080

spring:
  application:
    name: examendu

  datasource:
    url: jdbc:postgresql://localhost:5432/examendu
    username: examendu
    password: examendu123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never
